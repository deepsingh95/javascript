// while

let index = 0;

while (index <= 10) {
    // console.log(`value of index is ${index}`);
    index = index + 2;
}

let myArray = ["spiderman", "hulk", "batman"];

let arr = 0;

while (arr < myArray.length) {
    // console.log(`value of arr is ${myArray[arr]}`);
    arr = arr + 1;
}

// let score = 11;
let score = 1;

do {
    console.log(`value of score is ${score}`);
    score++;
} while (score <= 10);